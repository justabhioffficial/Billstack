name: Keep-Alive Render Ping

on:
  schedule:
    # Run every 5 minutes to prevent Render free-tier cold starts
    - cron: '*/5 * * * *'
  workflow_dispatch: # Allows manual trigger anytime from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping BillStack Render Backend
        run: |
          echo "Pinging https://mybillstack.onrender.com/api/v1/health to keep Render backend warm..."
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" https://mybillstack.onrender.com/api/v1/health)
          echo "Received HTTP Status Code: $HTTP_STATUS"
          curl -s https://mybillstack.onrender.com/v3/api-docs > /dev/null
          if [ "$HTTP_STATUS" -eq 200 ]; then
            echo "Render Backend is warm and healthy! 🚀"
          else
            echo "Ping returned status $HTTP_STATUS"
          fi
